export default function Page() {
  return (
    <div>
      Events - this should be a list of events purple has hosted or funded. Will replace with an API when the new
      eventcaster site is out
    </div>
  );
}
